You'll need to have:
    - npm & nodejs v16.x.x
    - docker engine
    - docker compose plugin
    - nginx - acts as reverse proxy
    - certbot - issues SSL certificates
    - a crontab that automatically re-news SSL certificates
